---
id: TASK-5
title: 'T-005: Wire `staging` branch + Vercel'
status: Done
assignee:
  - Codex
created_date: '2026-06-17 07:32'
updated_date: '2026-06-17 08:56'
labels:
  - imported-from-docs
  - phase-1
  - t-005
dependencies: []
references:
  - .docs/backlog.md
ordinal: 5000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Imported from .docs/backlog.md.
Source ID: T-005
Phase: Phase 1 — Lock & set up (Sessions 1–3, ~9h)
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 `git push origin main:staging` deploys a hello page.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Implement via Vercel Git integration, using the existing Next.js app as the hello page. Steps: 1. Verify local prerequisites and required environment variable names without exposing secrets. 2. Run local validation (`pnpm test`, `pnpm lint`, `pnpm build`). 3. Push `main` to remote `staging` with `git push origin main:staging`. 4. Verify whether Vercel access is available locally; if authenticated, inspect/confirm the deployment, otherwise document the required Vercel UI verification. 5. Record deployment evidence or blocker notes in this task. Acceptance criterion #1 is checked only after the Vercel branch deployment URL renders successfully.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Local validation before staging push: `pnpm test` passed (5/5). `pnpm lint` passed with 3 warnings in Convex generated files only. `pnpm build` initially hit a sandbox-only Turbopack permission error, then passed outside the default sandbox. Required Vercel env names present locally: `NEXT_PUBLIC_CONVEX_URL`, `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, `CLERK_SECRET_KEY`, `CLERK_JWT_ISSUER_DOMAIN`, plus Convex deployment/site URL values.

Deployment evidence: Vercel project `dispatch` was created under `mmeister86s-projects`, connected to GitHub repo `mmeister86/dispatch_saas`, patched to framework `nextjs`, install command `pnpm install --frozen-lockfile`, build command `pnpm build`. Required env vars were added to preview/staging and production because Vercel classified the staging Git deployment as target `production`. `git push origin main:staging` advanced `origin/staging` to commit `090897d0c4c4d4eeeb1f2c8dd693a70d61db3528` and produced a Vercel deployment. Initial deployment failed due missing production-target env vars; redeploy after env correction succeeded as `dpl_7akbWyah5vnua5W1zPyxqWaRoqhh`, READY, aliases: `dispatch-tau.vercel.app`, `dispatch-mmeister86s-projects.vercel.app`, and `dispatch-git-staging-mmeister86s-projects.vercel.app`. Verification: `curl -I https://dispatch-tau.vercel.app` returned HTTP 200; body contains `Dispatch` and `Clerk GitHub sign-in`. The generated branch alias remains protected by Vercel Authentication (HTTP 401 anonymously); authenticated `vercel curl` verified it also renders the Dispatch page. SSO protection was left enabled; the temporary automation bypass token generated by `vercel curl` was removed afterward.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Completed TASK-5 by wiring the Vercel project `dispatch` to GitHub repo `mmeister86/dispatch_saas`, configuring it as a Next.js project with `pnpm install --frozen-lockfile` and `pnpm build`, and setting the required Clerk/Convex environment variables. Verified `git push origin main:staging` triggers a Vercel deployment; latest staging deployment was READY and the deployed Dispatch hello/auth page returned HTTP 200 with expected content. Local validation passed: `pnpm test` 5/5, `pnpm lint` 0 errors with existing generated-file warnings, and `pnpm build` passed outside the sandbox. Deployment protection remains enabled for generated branch aliases; the public Vercel alias rendered successfully.
<!-- SECTION:FINAL_SUMMARY:END -->
